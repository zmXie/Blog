{
  "name": "xzm",
  "version": "1.0.0",
  "author": "xzm",
  "scripts": {
    "dev": "vuepress dev . --open --host \"localhost\"",
    "build": "NODE_OPTIONS='--openssl-legacy-provider --max-old-space-size=4096' NODE_ENV=production vuepress build .",
    "vercel-build": "npm run build",
    "deploy": "bash deploy.sh",
    "push": "bash push.sh"
  },
  "devDependencies": {
    "@vuepress/plugin-medium-zoom": "^1.9.7",
    "vuepress": "1.8.2",
    "vuepress-plugin-nuggets-style-copy": "^1.0.3",
    "vuepress-theme-reco": "1.6.10"
  },
  "description": "xzm",
  "engines": {
    "node": "20.x"
  }
}
